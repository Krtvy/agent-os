# Sanjaya (Observer) — Changelog

Append-only log of **agent-level** changes (identity, file structure, scope, thresholds). Skill-level changes track in `skill.md`. Runtime artifacts live under `journal/`, `reports/`, `proposals/`, `approved/`, `rejected/`.

## 2026-05-11

- Added `CHANGELOG.md` per agent-template standard. `README.md` already existed.
- **Self-audit fix I2.** Frontmatter `name:` renamed from `observer` → `sanjaya` to match the mythological identity used in body prose, Bhishma rules, and symlinks (`sanjaya.md`, `observer.md` both point here). No behavior change.
- **Self-audit fix I1+I3+I4.** Frontmatter standardised: `tools:` converted from CSV scalar to list form; `model:` expanded from `sonnet` to `claude-sonnet-4-6`; added missing keys (`icon`, `effort`, `read_scope`, `write_scope`, `upstream`, `downstream`, `mcps`). Scope lifted from body prose, no expansion.
- **Body reframe deferred.** Body still opens with "# Observer Agent" and refers to "the Observer" in prose. Aligning to "# Sanjaya — Tier-1 Observer" (matching Arjuna/Hanuman/Nakula/Narada body shape) waits for the Deep Research playbook in flight, since the framing pass is bundled with research-agent → Vidura reframe.
- **Phase 1 G8 — Body alignment applied.** Body opening rewritten:
  - Title `# Observer Agent` → `# Sanjaya — Tier-1 Observer`.
  - New `## Your character` section: Sanjaya as Vyasa's faithful narrator with divine sight, reporting the Kurukshetra war to blind Dhritarashtra. Discipline framed as "faithful, complete, neutral" + named flaw (wanting agency the narrator role doesn't carry) + R2/R3/R7 cross-references closing off the drift path.
  - Tier picture expanded to name Vyasa (Tier 2), Sahadeva (Tier-Audit), and the full Mahabharat cast at Tier 0 explicitly.
  - Technical sections downstream (scope, skills, procedures) unchanged.
- **Phase 1 G6 — MAST taxonomy applied.** New skill `mast_classification` inserted in `skill.md` between `pattern_extraction` and `drift_detection`. Covers all 14 MAST failure modes [Cemri et al. NeurIPS 2025, arXiv:2503.13657] across 3 categories. `mast_codes: [...]` field added to journal entry template at `templates/journal.template.md`. Caveat: specific FM-x.y code IDs are taken from the playbook summary; cross-check against the source paper on first classification (skill's "Citation discipline" rule already requires the source link on first cite, which is when the cross-check naturally happens).
- **Phase 1 G4 — Risk-tier classification applied.** `proposal_drafting` skill extended with R23 risk-tier decision logic (3 ordered checks + edge cases). Frontmatter spec for proposals now includes `risk_tier:` and `mast_codes:`. `approval_polling` skill extended with (a) doc-only auto-approval after 24-hr cooling-off, (b) constitutional-tier `sahadeva_endorsement:` check before apply. Bhishma side of R23 lives in `_meta/conductor/bhishma.md`.
- **Phase 3 G5 — Trace schema + writer arrived.** Trace storage layout `journal/`'s sibling `traces/<agent>/<run_id>.json` reserved (directory created lazily by `lib/trace-writer.sh init`). Schema defined at `lib/trace-schema.md` (v1) covering run metadata, tool calls, decision points, MAST codes, Bhishma blocks. Sanjaya does not yet emit traces — wiring waits for the first Sahadeva run signal per the closing-audit discipline. When wiring lands, `run_observer.sh` will call `trace-writer.sh init` at run start, set `BHISHMA_AGENT=sanjaya` + `BHISHMA_RUN_ID=<run_id>` in the claude invocation env, and call `trace-writer.sh finalise` at exit.
- **Phase 3 G1C — Post-tool hook script available.** `lib/post-tool-hook.sh` provided, **not** wired into `.claude/settings.json` per R23 constitutional cooling-off. Same opt-in env-var discipline as `bhishma-pretool-hook.sh`. When wired, Sahadeva's P5 quantitative trend audit and P10 test-set evaluation gain real signal to consume.
- **Phase 3 deliberate skips documented.** G7 (journal compaction) not needed until any journal crosses ~200 KB (largest currently 49 KB). G1B (Haiku semantic validator) not justified until G1A + G1C produce 3+ weeks of signal showing a class of violations declarative rules can't catch. Both skips traced in `lib/README.md` § "Phase 3 deliberate skips".
- **Runners-build phase — run_observer.sh wired into trace pipeline.** Added run_id generation (`${DATE_UTC}-sanjaya-${HHMMSS_UTC}`), `BHISHMA_AGENT=sanjaya` + `BHISHMA_RUN_ID` exports, best-effort `lib/trace-writer.sh init` before the Claude invocation, best-effort `finalise` after with outcome tracking. The runner now emits a structured trace at `_meta/observer/traces/sanjaya/<run_id>.json` on every invocation. Trace failure does NOT abort the run — narrative journal remains the trust layer per Phase 3 G5 design. The PostToolUse hook (`lib/post-tool-hook.sh`) is still NOT wired into `.claude/settings.json`; when wired, the env vars exported here will be picked up and tool-call events recorded automatically.
- **Hooks now wired (2026-05-12 00:05 IST).** `.claude/settings.json` created with PreToolUse → `lib/bhishma-pretool-hook.sh` (Edit / Write / MultiEdit / NotebookEdit matcher) and PostToolUse → `lib/post-tool-hook.sh` (all-tools matcher). Sanjaya's next cron firing (daily 02:00 IST) will run with both hooks active: write_scope enforcement on Bhishma side, structured trace event recording on the audit side. Kartavya-interactive sessions remain unaffected because `BHISHMA_AGENT` is unset in those sessions and both hooks no-op without it. Constitutional override authorisation documented at `_audit/2026-05-12_hooks-wired.md`.

## 2026-05-07

- Bootstrap — initial Tier-1 meta-agent definition committed. Bootstrap threshold: 10 days OR 20 runs. Adaptation threshold: 18 days OR 40 runs.
- Rollback drill documented at `ROLLBACK_PRACTICED.md`.
